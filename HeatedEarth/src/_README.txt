To compile:

Utilize the ArgumentFile to compile with Java: javac @ArgumentFile

To run:

Run with Java, utilizing the java command followed by the Name of the package (EarthSim), followed by ".Demo" and the list of arguments (for example: java EarthSim.Demo -s -p -r -b50)